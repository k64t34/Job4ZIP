/*
 * Created by SharpDevelop.
 * User: skorik
 * Date: 31.05.2018
 * Time: 15:01
 * 
 * To change this template use Tools | Options | Coding | Edit Standard Headers.
 */
using System;

namespace Job4ZIP
{
	/// <summary>
	/// Description of ReadFromXML.
	/// </summary>
	public class ReadFromXML
	{
		public ReadFromXML()
		{
		}
		//****************************************
		bool ReadStringFromConfig()
		//*************************************************		
		{
			/*Dim myel =From el In XmlDoc.<JOB>.Elements()  Where el.Name=argName Select el		 
			var=myel.value   
			If Strings.Left(var,1)="""" Then
				If Strings.Right(var,1)="""" Then
					var=Strings.Mid(var,2,Strings.Len(var)-2)
				End If
			End If*/
			return true ;//len(var)<>0
		}
	}
}
